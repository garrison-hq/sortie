services:
  nanofish:
    build: .
    ports:
      - '3470:3470'
    env_file:
      - .env
    environment:
      NANOFISH_HOST: 0.0.0.0
      NANOFISH_DATA_DIR: /data
    volumes:
      - nanofish-data:/data
    init: true
    restart: unless-stopped

volumes:
  nanofish-data:
