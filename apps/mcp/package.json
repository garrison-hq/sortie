{
  "name": "@garrison-hq/sortie-mcp",
  "version": "0.1.0",
  "license": "AGPL-3.0-only",
  "private": true,
  "description": "sortie MCP server (stdio) — web_outline, web_search, web_fetch, web_extract, run_agent, run_saved_query",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/garrison-hq/sortie.git",
    "directory": "apps/mcp"
  },
  "type": "module",
  "bin": {
    "sortie-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@garrison-hq/sortie": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.2"
  }
}
